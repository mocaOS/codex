import config from "@local/config";

/**
 * API base URL from the shared config package.
 * This ensures consistency across all documentation pages.
 */
export const API_BASE_URL = config.api.baseUrl;
